# Review Feature
Review `git diff` and task manifest. Verify every changed file is in scope. Then assess correctness, architecture, tests, security, accessibility, performance, AI safety and maintainability. Report severity-ranked findings; do not silently expand the diff.
