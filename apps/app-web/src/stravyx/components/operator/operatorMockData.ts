// Cash-out modal constants only. Live dashboard figures come from
// `dashboardStats.ts` + the operator's `recentJobs`. PAST_JOBS,
// WEEKLY_EARNINGS and PENDING_BALANCE were retired — they contradicted
// live/honest-empty shells.
export const EARLY_FEE_RATE = 0.025;
export const INSTANT_THRESHOLD = 1000;
export const NEXT_PAYOUT_DATE = "Not connected";
export const NEXT_PAYOUT_TIME = "Not connected";
