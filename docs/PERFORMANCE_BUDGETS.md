# Performance Budgets

Projects must define only the budgets that matter to their users: web vitals/bundle/image weight for web UI, p95 latency for synchronous APIs, and first-useful-response/total latency for AI. Budgets are targets and regression signals, not excuses for premature optimisation. Material regressions require evidence or an approved exception.
